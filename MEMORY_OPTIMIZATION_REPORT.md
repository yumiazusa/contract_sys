# 合同管理系统内存优化说明

## 1. 项目现状与结论

- 技术栈实际为 `Flask + SQLAlchemy + 原生 HTML/JS`，并非 Node.js 服务。
- 当前白屏与服务重启后短暂恢复的核心原因，不是单点“内存泄漏函数”，而是多处“全量数据常驻内存”叠加：
  - 后端接口一次性查询全部合同。
  - 前端页面一次性拉取全部合同并常驻浏览器内存。
  - Excel 导出在服务端内存中生成整份工作簿。
  - 生产运行仍使用开发模式启动，长时间运行下稳定性差。

## 2. 已定位的内存风险根因

### 后端

1. 合同列表接口 `GET /api/contracts`
   - 原逻辑使用 `.all()` 一次性加载全部合同记录。
   - 数据量增长后会导致 Python 进程瞬时占用升高，并增加 JSON 序列化内存成本。

2. Excel 导出接口 `GET /api/export/excel`
   - 原逻辑先把所有合同查到内存，再使用 `openpyxl + BytesIO` 在内存中构建完整 Excel。
   - 合同数量增加后，该接口会形成明显的峰值内存。

3. 数据库连接池配置缺失
   - 原逻辑未根据低配服务器做池化约束。
   - 在并发访问和异常网络波动下，连接长期占用会放大内存与句柄压力。

4. 运行参数不适合生产
   - 原逻辑直接 `debug=True` 启动。
   - 开发模式会带来额外的重载器与调试开销，不适合 2C4G 长时间运行。

### 前端

1. 页面全量持有合同数据
   - 原逻辑将全部合同保存到 `allContracts`，整个页面生命周期不释放。
   - 筛选、重渲染都在前端全量数组上进行，数据越大占用越高。

2. 全量 DOM 重建
   - 每次筛选都重建整个表格。
   - 在大列表场景下，会加剧浏览器内存抖动与白屏风险。

3. 新增/编辑后整页刷新
   - 使用 `location.reload()` 会导致页面反复重新加载所有数据。
   - 在低配机器和大数据量下，容易触发短时卡顿甚至白屏。

## 3. 本次代码优化内容

### 3.1 后端优化

- 将合同列表接口改为服务端分页查询：
  - 支持 `page`、`page_size`。
  - 支持 `keyword`、`contract_type`、`platform`、`filler`、`executive_partner`、`department` 服务端筛选。
  - 返回结构改为 `items + pagination`，避免一次性返回全量数据。

- 新增统计接口：
  - `GET /api/contracts/summary`
  - 用于首页统计卡片，避免为了统计而加载全部合同明细。

- 新增单条详情接口：
  - `GET /api/contracts/<id>`
  - 前端编辑时按需加载，替代“前端常驻全量数据后本地查找”。

- 优化 Excel 导出：
  - 改用 `openpyxl.Workbook(write_only=True)`。
  - 改用 `yield_per()` 分批读取数据库。
  - 改用临时文件落盘导出，避免 `BytesIO` 全量常驻内存。
  - 导出支持沿用当前筛选条件，减少不必要的全量导出。

- 优化数据库连接配置：
  - 新增 `.env` 驱动配置。
  - 为非 SQLite 场景增加：
    - `pool_pre_ping`
    - `pool_recycle`
    - `pool_size`
    - `max_overflow`
    - `pool_timeout`
  - 针对低配云主机建议默认池大小为 `3 + 2`。

- 增强会话与请求生命周期清理：
  - 明确在应用上下文结束后执行 `db.session.remove()`。

- 优化合同编号逻辑：
  - 年份由硬编码 `2026` 改为动态当前年份。
  - 新建合同时增加冲突重试，降低并发编号冲突风险。

- 生产启动参数改造：
  - `app.py` 不再固定 `debug=True`。
  - 支持通过环境变量控制 `FLASK_DEBUG`、`PORT`、`HOST`。
  - 提供 `gunicorn.conf.py` 作为 Linux 生产建议配置，支持 `max_requests` 周期性回收 worker，抑制长期运行后的内存膨胀。

### 3.2 前端优化

- 移除前端全量合同常驻缓存：
  - 用分页状态对象替代 `allContracts`。
  - 页面仅持有“当前页合同”。

- 筛选改为服务端查询：
  - 关键字、类型、平台、填表人、执行合伙人、部门不再在浏览器内存中全量过滤。

- 增加分页控件：
  - 支持 20 / 50 / 100 条每页。
  - 限制单次渲染 DOM 规模。

- 新增请求防抖与中断：
  - 输入筛选时增加 250ms 防抖。
  - 新请求发起时自动中止上一个列表请求，减少重复请求与无效内存开销。

- 编辑按需加载：
  - 编辑按钮改为即时获取单条详情，不再依赖全量数组驻留。

- 提交后局部刷新：
  - 创建、编辑、作废、删除后仅刷新统计与当前页数据，不再整页重载。

- 导出按筛选结果执行：
  - 减少无意义的超大导出请求。

## 4. 关键文件修改点

- 后端入口与接口改造：`app.py`
- 前端分页与按需加载：`templates/dashboard.html`
- 环境变量示例：`.env.example`
- Linux 生产启动配置：`gunicorn.conf.py`

## 5. Linux 服务器排查与验证命令

> 以下命令需在目标 Linux 服务器执行，用于补齐“优化前后对比数据”。

### 5.1 基础资源监控

```bash
free -m
top -c
htop
ps -eo pid,ppid,cmd,%mem,%cpu,rss,vsz --sort=-rss | head
```

### 5.2 进程级持续采样

将 `PID` 替换为实际 Python/Gunicorn 进程号：

```bash
while true; do
  date "+%F %T"
  ps -p PID -o pid,ppid,%cpu,%mem,rss,vsz,etime,cmd
  sleep 60
done >> /var/log/contract_system_mem.log
```

### 5.3 Gunicorn 运行建议

```bash
gunicorn -c gunicorn.conf.py app:app
```

### 5.4 浏览器端内存验证

1. 打开 Chrome DevTools。
2. 进入 `Memory` 面板。
3. 进入合同列表页，分别在“初次打开、分页翻页、筛选、导出前后”执行 Heap Snapshot。
4. 检查是否仍存在大数组常驻、Detached DOM、重复请求导致的数据对象滞留。

## 6. 72 小时验证方案

### 6.1 验证目标

- 服务连续运行 72 小时以上不出现白屏。
- 内存曲线稳定，无持续单向爬升。
- 登录、查询、筛选、创建、编辑、作废、删除、导出功能全部正常。

### 6.2 建议观测指标

- 系统级：
  - 总内存使用率
  - 可用内存
  - CPU 使用率
  - load average

- 进程级：
  - RSS
  - VSZ
  - 进程重启次数
  - 平均响应时间

- 业务级：
  - 登录成功率
  - 合同列表接口耗时
  - Excel 导出接口耗时
  - 页面白屏/JS 报错次数

### 6.3 推荐压测场景

1. 100 个并发用户持续执行登录与列表分页查询。
2. 高频执行筛选与翻页。
3. 间歇触发 Excel 导出。
4. 混合执行新增、编辑、作废、删除。

## 7. 优化前后对比数据模板

> 当前仓库环境不是目标 Linux 服务器，无法直接生成真实“优化前/优化后”生产数据。请按下表在服务器上补录实测值。

| 指标 | 优化前 | 优化后 | 目标 |
| --- | --- | --- | --- |
| Python 进程 RSS 峰值 | 待采集 | 待采集 | 显著下降 |
| 合同列表接口返回数据量 | 全量返回 | 按页返回 | 单次受控 |
| 浏览器首屏合同数据持有量 | 全量常驻 | 当前页常驻 | 显著下降 |
| Excel 导出峰值内存 | 待采集 | 待采集 | 显著下降 |
| 72 小时内是否白屏 | 是 | 待验证 | 否 |
| 72 小时内是否需重启 | 是 | 待验证 | 否 |

## 8. 后续长期维护建议

1. 对合同列表保持“分页优先”，禁止恢复全量返回。
2. 大批量导出建议升级为异步任务生成文件，避免高峰期占用 Web 进程。
3. 增加数据库索引审查，重点关注：
   - `created_at`
   - `contract_type`
   - `platform`
   - `filler`
   - `executive_partner`
   - `department`
4. 使用 `systemd + restart=always` 或容器健康检查，避免单次异常导致长期不可用。
5. 接入持续监控：
   - `Prometheus + Node Exporter`
   - `Grafana`
   - 进程级内存告警
6. 每月检查一次导出文件规模与接口慢查询，防止数据规模增长后再次退化。
